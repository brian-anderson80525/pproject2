{% layout "layout.liquid" %}

{% block content %}

<div class="new_b">
<a href="list/new"><button>Create New Item</button></a>
</div>
{% for ea in list %}

<div class=item>
    <p class="each"><a href="list/{{ea._id}}">{{ea.item}}</a></p>
        <div class="form">
                <form class="delete" action="/list/{{ ea._id }}?_method=DELETE" method="post">
                <input type="submit" value="Got It"></form>
        </div>

</div>


{% endfor %}


{% endblock %}

</form>